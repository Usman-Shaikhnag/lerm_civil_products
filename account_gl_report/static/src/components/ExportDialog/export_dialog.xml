<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="gl_report.ExportDialog">
        <div class="o_gl_export_modal" t-on-click="props.onClose">
            <div class="o_gl_export_content" t-on-click.stop="">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h5 class="mb-0"><i class="fa fa-download me-2"/>Export Report</h5>
                    <button class="btn btn-sm btn-close" t-on-click="props.onClose"/>
                </div>
                <p class="text-muted small mb-3">
                    Export <strong><t t-esc="props.totalRecords"/></strong> records to:
                </p>

                <div class="o_gl_export_btn" t-on-click="() => props.onExport('csv')"
                     t-att-class="props.exporting ? 'pe-none opacity-50' : ''">
                    <i class="fa fa-file-text-o"/>
                    <div>
                        <div class="o_gl_export_label">CSV</div>
                        <div class="o_gl_export_desc">Comma-separated values (.csv)</div>
                    </div>
                </div>

                <div class="o_gl_export_btn" t-on-click="() => props.onExport('xlsx')"
                     t-att-class="props.exporting ? 'pe-none opacity-50' : ''">
                    <i class="fa fa-file-excel-o"/>
                    <div>
                        <div class="o_gl_export_label">Excel</div>
                        <div class="o_gl_export_desc">Microsoft Excel (.xlsx)</div>
                    </div>
                </div>

                <div class="o_gl_export_btn" t-on-click="() => props.onExport('pdf')"
                     t-att-class="props.exporting ? 'pe-none opacity-50' : ''">
                    <i class="fa fa-file-pdf-o"/>
                    <div>
                        <div class="o_gl_export_label">PDF</div>
                        <div class="o_gl_export_desc">Portable Document Format (.pdf)</div>
                    </div>
                </div>

                <div t-if="props.exporting" class="text-center mt-2">
                    <i class="fa fa-spinner fa-spin me-1"/> Exporting...
                </div>
            </div>
        </div>
    </t>

</templates>
